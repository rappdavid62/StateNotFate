warning: in the working copy of '.github/copilot-instructions.md', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/.github/copilot-instructions.md b/.github/copilot-instructions.md[m
[1mindex 7de5917..e50e559 100644[m
[1m--- a/.github/copilot-instructions.md[m
[1m+++ b/.github/copilot-instructions.md[m
[36m@@ -2,28 +2,35 @@[m
 [m
 This repository is a static PWA / public website. Treat it as a plain HTML/CSS/JavaScript app, not a framework or backend project.[m
 [m
[31m-## What to know before editing[m
[31m-- Primary source of truth: `AGENTS.md`.[m
[31m-- Secondary context: `README.md` and `docs/AI-Systems-MOC.md`, `docs/State-Not-Fate-MOC.md`.[m
[31m-- Preserve the static PWA structure: `index.html`, `index.css`, `app.js`, `service-worker.js`, `manifest.json`, and `netlify.toml` are core assets.[m
[31m-- Do not change application architecture, migrate the app to a different stack, or add a backend.[m
[31m-- Do not change `index.html`, `app.js`, `index.css`, `service-worker.js`, `package.json`, or `package-lock.json` unless the user explicitly requests it.[m
[32m+[m[32m## Source of truth[m
[32m+[m[32m- Primary source of truth: AGENTS.md.[m
[32m+[m[32m- Secondary context: README.md, docs/AI-Systems-MOC.md, and docs/State-Not-Fate-MOC.md.[m
[32m+[m[32m- Core app/deployment files include index.html, index.css, app.js, service-worker.js, manifest.json, package.json, package-lock.json, and netlify.toml.[m
[32m+[m
[32m+[m[32m## Hard rules[m
[32m+[m[32m- Do not migrate the app to React, TypeScript, a backend, or another framework unless explicitly requested.[m
[32m+[m[32m- Do not change application architecture unless explicitly requested.[m
[32m+[m[32m- Do not invent file paths.[m
[32m+[m[32m- Do not change core app files unless the user explicitly approves the plan.[m
[32m+[m[32m- Preserve browser localStorage state behavior; there is no backend state.[m
[32m+[m[32m- Preserve the static Netlify deployment assumption and fallback redirect to index.html.[m
 [m
 ## Safe workflow rules[m
[31m-1. Inspect first: review the relevant files and tests before editing.[m
[31m-2. Plan before edits: summarize the proposed change, affected files, and validation path.[m
[31m-3. Keep diffs small: prefer targeted modifications and avoid broad refactors.[m
[31m-4. Preserve safety and accessibility: maintain crisis content, public-facing site integrity, SEO metadata, and PWA behavior.[m
[31m-5. Use existing tests: `npm test` and `npm run test:public` are the trusted validation steps.[m
[31m-6. Roll back if needed: commit or snapshot before changes, and use git to revert any unsafe edits.[m
[32m+[m[32m1. Inspect relevant files before editing.[m
[32m+[m[32m2. Produce a short plan before edits.[m
[32m+[m[32m3. Name affected files before making changes.[m
[32m+[m[32m4. Keep diffs small and reviewable.[m
[32m+[m[32m5. Avoid unrelated refactors.[m
[32m+[m[32m6. Preserve safety/crisis content, accessibility, SEO metadata, public site behavior, and PWA behavior.[m
[32m+[m[32m7. Include acceptance criteria, validation steps, and rollback notes.[m
[32m+[m[32m8. Ask for clarification when the requested change is ambiguous.[m
 [m
 ## Validation guidance[m
[31m-- `npm run serve` to start the local static server from `scripts/static-server.mjs`.[m
[31m-- `npm test` to run the full Playwright suite.[m
[31m-- `npm run test:public` to run the public-facing Playwright tests.[m
[32m+[m[32m- npm run serve starts the local static server from scripts/static-server.mjs.[m
[32m+[m[32m- npm test runs the full Playwright suite.[m
[32m+[m[32m- npm run test:public runs the public-facing Playwright tests.[m
 [m
 ## Notes for future agents[m
[31m-- This repo stores state in browser `localStorage`; there is no backend state.[m
[31m-- Edits to `index.html` may affect both the public landing page and the embedded app experience.[m
[31m-- `netlify.toml` uses a static redirect fallback to `index.html`; preserve this deployment assumption.[m
[31m-- When in doubt, ask for clarification before applying a change.[m
[32m+[m[32m- Edits to index.html may affect both the public landing page and the embedded app experience.[m
[32m+[m[32m- Larger system or evidence-related changes should reference docs/AI-Systems-MOC.md and docs/State-Not-Fate-MOC.md.[m
[32m+[m[32m- When in doubt, ask before applying a change.[m
