# State Not Fate Agent Ledger

Record only actual findings and state transitions.

| ID | Status | Problem | Evidence | Root cause | Change | Verification | Sources | Commit | Rollback | Next transition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CONN-001 | VERIFIED_LOCAL | Unhashed `app.js` and `index.css` were served with a 1-year immutable browser cache, so deploys could pair new HTML with stale recovery JS/CSS. | Production `curl -sI https://statenotfate.netlify.app/app.js` returned `cache-control: public,max-age=31536000,immutable` (content-length 490878, matching this checkout). HTML used `max-age=0, must-revalidate`. | `netlify.toml` overrode Netlify's default browser revalidation with immutable rules meant for content-hashed filenames. This repo does not hash `app.js` / `index.css`. | Set `/*.js` and `/*.css` to `public, max-age=0, must-revalidate`. Bumped service-worker cache `v9` → `v10`. Added unit and health-check guards. | Local: `npm run test:unit`, `npm run health` 40/40, `npm test` 36 Playwright passed. Deploy preview `https://deploy-preview-15--statenotfate.netlify.app/app.js` returns `cache-control: public,max-age=0,must-revalidate`. Production still immutable until merge. | Netlify caching docs: immutable only for fingerprinted assets. | `7c41e9c` | Revert that commit; SW returns to `v9`. | After SNF merge, re-curl production `app.js` and confirm it is no longer `immutable`. |
| BUG-001 | VERIFIED_LOCAL | Node.js CI on SNF failed: suicide-prevention spec expected heading "source backbone". | GitHub run `31543467441` (2026-08-11): 35 passed, 1 failed at `suicide-prevention.spec.ts:11`. Local reproduce: heading not found. After heading fix, the same spec then failed on `/graphic detail\|method-specific/i` because the page teaches people not to publish those things. | Page heading was renamed to Source Atlas. Negative regex treated safe-messaging prohibitions as forbidden copy. | Assert Source Atlas. Require "Avoid language like… method-specific descriptions" and "Do not… publish graphic detail". Tag the spec `@critical`. | `npx playwright test tests/public/suicide-prevention.spec.ts` passed. Full `npm test`: 36 passed. | Page copy in `suicide-prevention.html` (safe messaging / avoid language panels). No new clinical claims. | `0e1c98e`, `f866dc5` | Revert those commits. | Confirm GitHub Actions Node.js CI green on PR 15. |
| CONN-002 | VERIFIED_LOCAL | `/essays` is in site nav and works on Netlify pretty URLs, but local static server, `netlify.toml`, and `sitemap.xml` omitted it. | Production `/essays` HTTP 200, content-length 19373 matching `essays.html`. `scripts/static-server.mjs` had no `/essays` map. Sitemap listed `/`, `/suicide-prevention`, `/evidence`, `/contact`, `/crisis` only. | Incomplete pretty-URL wiring after `essays.html` was added. | Explicit `/essays` → `/essays.html` rewrite, local server map, sitemap URL, unit test, presence-seo coverage. | Deploy preview `/essays` HTTP 200, title `Essays & Core Philosophy - State Not Fate`. Local Playwright presence-seo passed. | None. | `7c41e9c` | Revert that commit's essays hunks. | Optional: add `/essays` to production sitemap verification after merge. |
| CONN-003 | VERIFIED_LOCAL | `npm run health` failed 6 checks: missing hourly monitor workflow plus untagged specs. | Local health: missing `.github/workflows/hourly-synthetic-monitor.yml`. README still badged that workflow. David deleted the monitor in `eea93d9` / `a77e50b`. `crisis-safebox.spec.ts` and `suicide-prevention.spec.ts` lacked `@critical`/`@advisory` tags. | Health check and README were not updated when the monitor was removed. | Drop monitor file requirements. Badge existence check. Tag remaining specs. Remove dead README badge. | `npm run health`: 40 passed, 0 failed. | None. Monitor was not restored (David deleted it). | `0e1c98e` | Revert that commit. | Do not recreate hourly production Playwright without an explicit request. |
